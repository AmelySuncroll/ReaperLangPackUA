# Історія оновлень перекладу

**Версія 1.28d:**
- виправила вікно блокування та підказки, також оновила назви відповідних дій;
- виправила переклад і масштаб вікна «Вирівняти елементи за фазою» (дякувати Кені Джоя: https://youtu.be/7I5wy2sJsxo?si=DJO_VqvfxgsxdhLS);
- тощо.


**Версія 1.27:**
- вилучила агента, що зробив цей файл - далі я сама;
- додала 18 нових рядків відповідно до нової версії REAPER;
- тощо.

**Версія 1.27d:**
- змінила пару слів/речень у NINJAM (наприклад, там було "відправити" (повідомлення в чат), я змінила на "надіслати" тощо);
- у діях "перемкнути [щось]" тепер "увімкнути/вимкнути [щось]", якщо мається на увазі "працює/не працює". Залишила: "перемкнути заглушення/соло/виділення/тощо";
- натрапила на дії-перемикачи, які навіть в оригіналі не були позначені як toggle, і додала до них позначку [П];
- покращила словник синонімів;
- тощо.


**Версія 1.26:**
- виправила одруки (наприклад: «Сітка: встановити поділ **сітки сітки** за частотою кадрів» → «Сітка: встановити поділ **сітки** за частотою кадрів»);
- відредагувала масштаб деяких вікон (наприклад: common);
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Семпл-редагування: спектрально **відновити** значення семплу першого виділеного елемента в межах виділення часу…» → «Семпл-редагування: спектрально **виправити** значення семплу першого виділеного елемента в межах виділення часу…»);
- додала 48 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.25:**
- "ввімкнути" тепер "увімкнути" без винятків, бо вже декілька людей написали, що плутали слова вимкнути і ввімкнути, коли швидко читали;
- виправила родовий відмінок у термінах (маркер**у** тепер маркер**а**, екран**у** - екран**а** тощо);
- замінила «видалити» на «вилучити» для більшої узгодженості в діях (наприклад: «**Видалити** дублі з негативними емоджі» → «**Вилучити** дублі з негативними емоджі»);
- провела масштабну редактуру термінології у розділах actions, mousemaps, midi_actions (наприклад: «Інше: вставити **новий** клік-елемент» → «Інше: вставити клік-елемент», «Вилучати **точки** циклу» → «Вилучати **область** циклу»);
- переклала 226 абсолютно нових рядків (нові вікна, дії та меню з останніх оновлень REAPER);
- тощо.

**Версія 1.25c:**
- виправила родовий відмінок у термінах (наприклад: «Не вмикати бережник **екрану**...» → «Не вмикати бережник **екрана**...», «LICEcap — це простий безкоштовний додаток для захоплення **екрану** у форматі gif, створений Cockos» → «LICEcap — це простий безкоштовний додаток для захоплення **екрана** у форматі gif, створений Cockos»);
- замінила «видалити» на «вилучити» для більшої узгодженості в діях (наприклад: «**Видалити** емоджі-маркери дубля на позиції курсора редагування» → «**Вилучити** емоджі-маркери дубля на позиції курсора редагування»);
- провела масштабну редактуру термінології у розділах midi_actions, actions, mousemaps (наприклад: «Інше: вставити **ноту: Х**» → «Інше: вставити **ноту довжиною Х**», «Маркери: вирівняти **темпо-**маркери за роздільністю MIDI» → «Маркери: вирівняти маркери **темпу** за роздільністю MIDI»);
- тощо.

**Версія 1.25d:**
- замінила слово «майстер» на оригінальне «master» (наприклад, master-доріжка) (наприклад: «Відображати **майстер**-доріжку…» → «Відображати **master**-доріжку…»);
- провела масштабну редактуру термінології у розділах actions, xfade_actions (наприклад: «Редагування елемента: **посунути** елементи/точки обвідної ліворуч» → «Редагування елемента: **перемістити** елементи/точки обвідної ліворуч», «Передслуховування: **відтворити проєкт**» → «Передслуховування: **почати відтворення проєкту**»);
- тощо.


**Версія 1.24:**
- виправила родовий відмінок у термінах (наприклад: «Копіювати набір маркерів до **буферу** обміну» → «Копіювати набір маркерів до **буфера** обміну», «Розмір **буферу**:» → «Розмір **буфера**:»);
- провела масштабну редактуру термінології у розділах actions (наприклад: «Вкладки проєктів: перейти до вкладки Х» → «Вкладки проєктів: перейти до вкладки **проєкту** Х»);
- додала 79 нових рядків відповідно до нової версії REAPER;
- тощо.

**Версія 1.24b:**
- відредагувала масштаб деяких вікон (наприклад: wavpack);
- провела масштабну редактуру термінології у розділах actions, midi_actions, explorer_actions (наприклад: «Вигляд: перейти до **курсору** редагування» → «Вигляд: перейти до **курсора** редагування», «CC: наступна **CC**-смуга» → «CC: наступна **СС**-смуга»);
- додала 32 нових рядків відповідно до нової версії REAPER;
- тощо.

**Версія 1.24c:**
- провела масштабну редактуру термінології у розділах mousemaps, actions, explorer_actions (наприклад: «Ігноруючи прив**'**язку до сітки» → «Ігноруючи прив**’**язку до сітки», «Обвідна: вилучити **'**зайві**'** точки» → «Обвідна: вилучити **«**зайві**»** точки»);
- тощо.

**Версія 1.24d:**
- провела масштабну редактуру термінології у розділах undo, midi_undo, mousemaps (наприклад: «**Увімк/вимк** FX» → «**Перемкнуто** FX», «**Відрегульовано** край ноти» → «**Відредаговано** край ноти»);
- тощо.


**Версія 1.23:**
- замінила три крапки (трьома символами) на один символ «…» для охайності (наприклад: «**...**» → «**…**»);
- замінила «видалити» на «вилучити» для більшої узгодженості в діях (наприклад: «**Видалити** всі миттєві знятки» → «**Вилучити** всі миттєві знятки»);
- провела масштабну редактуру термінології у розділах actions (наприклад: «Вигляд: масштабувати за вмістом проєкту» → «Вигляд: масштабувати за вмістом проєкту **(out project)**»);
- додала 28 нових рядків відповідно до нової версії REAPER;
- тощо.

**Версія 1.23d:**
- відредагувала масштаб деяких вікон (наприклад: wavpack, sws_menu);
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Обрізати медіа **із розміром** хвоста:» → «Обрізати медіа, **розмір** хвоста:»);
- переклала 169 абсолютно нових рядків (нові вікна, дії та меню з останніх оновлень REAPER);
- тощо.


**Версія 1.22:**
- замінила три крапки (трьома символами) на один символ «…» для охайності (наприклад: «Оглянути**...**» → «Оглянути**…**»);
- провела масштабну редактуру термінології у розділах explorer_actions, actions (наприклад: «Інше: редагувати **теґ** метаданих: дата» → «Інше: редагувати **тег** метаданих: дата», «Сітка [П]: увімкнути **свінґ**» → «Сітка [П]: увімкнути **свінг**»);
- переклала 715 абсолютно нових рядків (нові вікна, дії та меню з останніх оновлень REAPER);
- тощо.


**Версія 1.21:**
- провела масштабну редактуру термінології у розділах actions (наприклад: «Дубль [П]: **розпочинати семпл-**редагування (якщо **наблизити**)» → «Дубль [П]: **вимкнути (bypass)** редагування **семплу виділеного дубля/елемента** (якщо **відредаговано**)»);
- переклала 124 абсолютно нових рядків (нові вікна, дії та меню з останніх оновлень REAPER);
- тощо.


**Версія 1.20:**
- "емодзі" замінила на "емоджі" (за системою Олександра Коваленка, наприклад: «Елемент: вилучити **емодзі**-маркери» → «Елемент: вилучити **емоджі**-маркери»);
- перевірила слова з позначками ?_ ХЗ_ НУХЗ_ тощо, у перекладі яких я не була впевнена;
- вичитала рядки журналу дій - тепер дієслово у 95% рядках стоїть першим: додано/створено/вилучено/переупорядковано ... ;
- переклала довідки з рендерингу, післяобробки рендерингу тощо;
- вичитала вікно live-запису та декількох інших;
- відредагувала масштаб деяких вікон;
- тощо.


**Версія 1.19:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Вихід OpenGL є **рекомендований**. Інші виходи - дуже повільні, але більш сумісні - також доступні.» → «Вихід OpenGL є **рекомендованим**. Інші виходи - дуже повільні, але більш сумісні - також доступні.», «Опція: відкрити FX-**лінцюжок** у переглядачі файлів системи» → «Опція: відкрити FX-**ланцюжки** у переглядачі файлів системи»);
- тощо.


**Версія 1.18:**
- замінила слово «майстер» на оригінальне «master» (наприклад, master-доріжка);
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Нормалізація кожного **елемента** окремо: регулювання гучності кожного **елемента** відповідно його власним піковим значенням або сприйманої гучності.» → «Нормалізація кожного **файлу** окремо: регулювання гучності кожного **файлу** відповідно його власним піковим значенням або сприйманої гучності.», «Накладання: **накладення** зображення» → «Накладання: **накладання** зображення»);
- додала 71 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.17:**
- провела масштабну редактуру термінології у розділах actions, render, menus (наприклад: «FX [П]: **відокремлювати** нові FX-вікна» → «FX [П]: **автоматично відкривати** нові FX-вікна **окремо від FX-ланцюжка**», «3. **Фейд**-ін/аут» → «3. **Додаємо фейд**-ін/аут»);
- додала 21 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.16:**
- внесла дрібні виправлення та покращення до наявного перекладу;
- тощо.


**Версія 1.15:**
- провела масштабну редактуру термінології у розділах transport, actions (наприклад: «**?_Відстежуємо**» → «**Відстежуємо**», «Вигляд: відкрити **Навігатор**» → «Вигляд: відкрити **мапу проєкту**»);
- додала 2 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.14:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Додати смужку **(lane)**» → «Додати смужку», «Рендеринг кожної виділеної доріжки як **окремий файл**.» → «Рендеринг кожної виділеної доріжки як **окремого файлу**.»);
- додала 69 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.13:**
- провела масштабну редактуру термінології у розділах mousemaps (наприклад: «**Перемиково виділяти** СС» → «**Перемикати виділення** СС»);
- тощо.


**Версія 1.12:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «**JSFX назви** файлів» → «**Назви** файлів **JSFX**», «**Показати** у списку FX» → «**Відображати** у списку FX»);
- додала 38 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.11:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Українська, v1.**1**  |  Офіційний сайт:» → «Українська, v1.**11**  |  Офіційний сайт:»);
- тощо.


**Версія 1.10:**
- провела масштабну редактуру термінології у розділах mousemaps (наприклад: «Рухати вміст регіону **проєкту**» → «Рухати вміст регіону»);
- додала 47 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.09:**
- провела масштабну редактуру термінології у розділах mousemaps (наприклад: «**Інвертувати**» → «**Інвертовано**»);
- переклала 178 абсолютно нових рядків (нові вікна, дії та меню з останніх оновлень REAPER);
- тощо.


**Версія 1.08:**
- провела масштабну редактуру термінології у розділах explorer_actions (наприклад: «Інше: сортувати список файлів за **стопцем**: 'Дата'» → «Інше: сортувати список файлів за **стовпцем**: 'Дата'»);
- тощо.


**Версія 1.07:**
- провела масштабну редактуру термінології у розділах fxmenu (наприклад: «Навчити» → «Навчити**...**»);
- тощо.


**Версія 1.06:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Перемотка: поміняти місцями крайні значення діапазону **зацикленої миті**» → «Перемотка: поміняти місцями крайні значення діапазону **миті, що передслуховується**», «Обрізати медіа, **розмір** хвоста:» → «Обрізати медіа **із розміром** хвоста:»);
- додала 6 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.05:**
- відредагувала та покращила понад 55 рядків, виправляючи неточності та стилістичні помилки (наприклад: «**Поведінка опції** \"Відкрити елементи в MIDI-редакторі\"» → «**Дія** \"Відкрити елементи в MIDI-редакторі\" **відкриває**», «Дозволити надмінімальну висоту лінійки (сховати смужки маркерів **та** регіонів)» → «Дозволити надмінімальну висоту лінійки (сховати смужки маркерів **і** регіонів)»);
- додала 6 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.04:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Зберегти **одиночне меню:**» → «Зберегти **меню [лише одне]:**», «Області редаг. лезом**,** як один файл» → «Області редаг. лезом **(**як один файл**)**»);
- додала 74 нових рядків відповідно до нової версії REAPER;
- тощо.


**Версія 1.03:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Прив'язувати за будь-**який** відстані» → «Прив'язувати за будь-**якої** відстані», «Експортувати сполучення клавіш/власні дії **всіх секцій...**» → «Експортувати сполучення клавіш/власні дії **(всі секції)...**»);
- тощо.

**Версія 1.02:**
- провела масштабну редактуру термінології у розділах actions (наприклад: «Інше: задати темп проєкту за виділенням часу (**із** наявним темпом)» → «Інше: задати темп проєкту за виділенням часу (**з** наявним темпом)»);
- тощо.


**Версія 1.01:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Спрямовувати до вводу **віртуальної** міді-клавіатури» → «Спрямовувати до вводу **вірт.** міді-клавіатури», «**Завжди** редагувати елементи поза активним» → «**Дозволити** редагувати елементи поза активним»);
- тощо.


**Версія 1.01:**
- перша публічна не-бета версія.