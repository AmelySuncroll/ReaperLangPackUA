# Історія оновлень перекладу


**Версія 1.28c:**
- знайшла останні дев’ять випадків написання «**точки** циклу» - виправила на «**область** циклу»;
- «Хвилини:Секунди **(мінімально)**» вирішила замінити на «Хвилини:Секунди:**Мілісекунди**»;
- змінила рядкі «... **замінити** форму фейд-...» на «... **встановити** форму фейд-...» для узгодженості;
- виправила «розподілити ... **на**» на «розподілити ... **по**» (наприклад: «розподілити **на нові доріжки**» → «розподілити **по нових доріжках**» тощо);
- виправила один випадок «прикріпити» на «закріпити»;
- змінила пунктуаційно рядки дій з редагування метаданих (наприклад: «Інше: редагувати тег метаданих: **коментар**» → «Інше: редагувати тег метаданих: **„Коментар“**» тощо) для узгодженості;
- тощо.

**Версія 1.28d:**
- виправила вікно блокування та підказки, також оновила назви відповідних дій;
- виправила переклад і масштаб вікна «Вирівняти елементи за фазою» (дякувати Богові, що випадково натрапила на нього, та Кені Джоя: https://youtu.be/7I5wy2sJsxo?si=DJO_VqvfxgsxdhLS);
- тощо.


**Версія 1.27:**
- вилучила агента, що зробив цей файл - далі я сама;
- оновила переклад відповідно до нової версії REAPER;
- тощо.

**Версія 1.27d:**
- змінила пару слів/речень у NINJAM (наприклад: «**відправити**» → «**надіслати** [повідомлення в чат]» тощо);
- у діях «перемкнути [щось]» тепер «увімкнути/вимкнути [щось]», якщо мається на увазі «працює/не працює». Залишила: «перемкнути заглушення/соло/виділення/тощо»;
- натрапила на дії-перемикачи, які навіть в оригіналі не були позначені як toggle, і додала до них позначку [П];
- тощо.


**Версія 1.26:**
- виправила одруки (наприклад: «Сітка: встановити поділ **сітки сітки** за частотою кадрів» → «Сітка: встановити поділ **сітки** за частотою кадрів»);
- відредагувала масштаб деяких вікон (наприклад: common);
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Семпл-редагування: спектрально **відновити** значення семплу першого виділеного елемента в межах виділення часу…» → «Семпл-редагування: спектрально **виправити** значення семплу першого виділеного елемента в межах виділення часу…»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.25:**
- «ввімкнути» тепер «увімкнути» без винятків, бо вже декілька людей написали, що плутали слова вимкнути і ввімкнути, коли швидко читали;
- виправила родовий відмінок у деякіх словах (маркер**у** тепер маркер**а**, екран**у** - екран**а** тощо);
- відредагувала деякі рядки у розділах actions, mousemaps, midi_actions (наприклад: «Інше: вставити **~~новий~~** клік-елемент» → «Інше: вставити клік-елемент», «Вилучати **точки** циклу» → «Вилучати **область** циклу»);
- переклала 226 нових рядків відповідно до нової версії REAPER;
- тощо.

**Версія 1.25c:**
- замінила «видалити» на «вилучити» для більшої узгодженості в діях (наприклад: «**Видалити** емоджі-маркери дубля на позиції курсора редагування» → «**Вилучити** емоджі-маркери дубля на позиції курсора редагування»);
- відредагувала деякі рядки у розділах midi_actions, actions, mousemaps (наприклад: «Інше: вставити **ноту: Х**» → «Інше: вставити **ноту довжиною Х**», «Маркери: вирівняти **темпо-**маркери за роздільністю MIDI» → «Маркери: вирівняти маркери **темпу** за роздільністю MIDI»);
- тощо.

**Версія 1.25d:**
- замінила слово «майстер» на оригінальне «master» (наприклад, master-доріжка) (наприклад: «Відображати **майстер**-доріжку…» → «Відображати **master**-доріжку…»);
- відредагувала деякі рядки у розділах actions, xfade_actions (наприклад: «Редагування елемента: **посунути** елементи/точки обвідної ліворуч» → «Редагування елемента: **перемістити** елементи/точки обвідної ліворуч», «Передслуховування: **відтворити проєкт**» → «Передслуховування: **почати відтворення проєкту**»);
- тощо.


**Версія 1.24:**
- замінила символ **'** на **’**;
- замінила символ **-** на **—**;
- замінила лапки **" "** на **« »**;
- замінила крапки, що були трьома символами, на один спеціальний символ: «…»;
- виправила родовий відмінок у деякіх словах (наприклад: «Копіювати набір маркерів до буфер**у** обміну» → «Копіювати набір маркерів до буфер**а** обміну», «Вигляд: перейти до **курсору** редагування» → «Вигляд: перейти до **курсора** редагування»);
- відредагувала деякі рядки у розділах actions (наприклад: «Вкладки проєктів: перейти до вкладки Х» → «Вкладки проєктів: перейти до вкладки **проєкту** Х»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.

**Версія 1.24b:**
- відредагувала масштаб деяких вікон (наприклад: wavpack);
- оновила переклад відповідно до нової версії REAPER;
- тощо.

**Версія 1.24c:**
- невеличке оновлення, присутні незначні поліпшення перекладу.

**Версія 1.24d:**
- відредагувала деякі рядки у розділах undo, midi_undo, mousemaps (наприклад: «**Увімк/вимк** FX» → «**Перемкнуто** FX», «**Відрегульовано** край ноти» → «**Відредаговано** край ноти»);
- тощо.


**Версія 1.23:**
- замінила «видалити» на «вилучити» для більшої узгодженості в діях (наприклад: «**Видалити** всі миттєві знятки» → «**Вилучити** всі миттєві знятки»);
- відредагувала деякі рядки у розділах actions (наприклад: «Вигляд: масштабувати **за вмістом проєкту**» → «Вигляд: масштабувати **за вмістом проєкту (out project)**»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.

**Версія 1.23d:**
- відредагувала масштаб деяких вікон (наприклад: wavpack, sws_menu);
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Обрізати медіа **із розміром** хвоста:» → «Обрізати медіа, **розмір** хвоста:»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.22:**
- замінила три крапки (трьома символами) на один символ «…» для охайності (наприклад: «Оглянути**...**» → «Оглянути**…**»);
- відредагувала деякі рядки у розділах explorer_actions, actions (наприклад: «Інше: редагувати **теґ** метаданих: дата» → «Інше: редагувати **тег** метаданих: дата», «Сітка [П]: увімкнути **свінґ**» → «Сітка [П]: увімкнути **свінг**»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.21:**
- оновила переклад відповідно до нової версії REAPER;
- внесла дрібні виправлення та покращення у наявний переклад.


**Версія 1.20:**
- «емодзі» замінила на «емоджі» (за системою Олександра Коваленка, наприклад: «Елемент: вилучити **емодзі**-маркери» → «Елемент: вилучити **емоджі**-маркери»);
- замінила «**За**рендерити» на «**Від**рендерити» тощо;
- перевірила слова з позначками ?_ ХЗ_ НУХЗ_ тощо, у перекладі яких я не була впевнена;
- вичитала журнал дій - тепер дієслово у 95% рядках стоїть першим: додано/створено/вилучено/переупорядковано ... ;
- переклала довідки з рендерингу, післяобробки рендерингу тощо;
- вичитала вікно live-запису та декількох інших;
- відредагувала масштаб деяких вікон;
- тощо.


**Версія 1.19:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Вихід OpenGL є **рекомендований**. Інші виходи - дуже повільні, але більш сумісні - також доступні.» → «Вихід OpenGL є **рекомендованим**. Інші виходи - дуже повільні, але більш сумісні - також доступні.», «Опція: відкрити FX-**лінцюжок** у переглядачі файлів системи» → «Опція: відкрити FX-**ланцюжки** у переглядачі файлів системи»);
- тощо.


**Версія 1.18:**
- замінила слово «майстер» на оригінальне «master» (наприклад, master-доріжка);
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Нормалізація кожного **елемента** окремо: регулювання гучності кожного **елемента** відповідно його власним піковим значенням або сприйманої гучності.» → «Нормалізація кожного **файлу** окремо: регулювання гучності кожного **файлу** відповідно його власним піковим значенням або сприйманої гучності.», «Накладання: **накладення** зображення» → «Накладання: **накладання** зображення»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.17:**
- відредагувала деякі рядки у розділах actions, render, menus (наприклад: «FX [П]: **відокремлювати** нові FX-вікна» → «FX [П]: **автоматично відкривати** нові FX-вікна **окремо від FX-ланцюжка**», «3. **Фейд**-ін/аут» → «3. **Додаємо фейд**-ін/аут»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.16:**
- внесла дрібні виправлення та покращення у наявний переклад.


**Версія 1.15:**
- відредагувала деякі рядки у розділах transport, actions (наприклад: «**?_Відстежуємо**» → «**Відстежуємо**», «Вигляд: відкрити **Навігатор**» → «Вигляд: відкрити **мапу проєкту**»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.14:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Додати смужку **(lane)**» → «Додати смужку», «Рендеринг кожної виділеної доріжки як **окремий файл**.» → «Рендеринг кожної виділеної доріжки як **окремого файлу**.»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.13:**
- відредагувала деякі рядки у розділах mousemaps (наприклад: «**Перемиково виділяти** СС» → «**Перемикати виділення** СС»);
- тощо.


**Версія 1.12:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «**JSFX назви** файлів» → «**Назви** файлів **JSFX**», «**Показати** у списку FX» → «**Відображати** у списку FX»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.11:**
- оновила переклад відповідно до нової версії REAPER;
- внесла дрібні виправлення та покращення у наявний переклад.


**Версія 1.10:**
- відредагувала деякі рядки у розділах mousemaps (наприклад: «Рухати вміст регіону **проєкту**» → «Рухати вміст регіону»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.09:**
- відредагувала деякі рядки у розділах mousemaps (наприклад: «**Інвертувати**» → «**Інвертовано**»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.08:**
- відредагувала деякі рядки у розділах explorer_actions (наприклад: «Інше: сортувати список файлів за **стопцем**: „Дата“» → «Інше: сортувати список файлів за **стовпцем**: „Дата“»);
- тощо.


**Версія 1.07:**
- відредагувала деякі рядки у розділах fxmenu (наприклад: «Навчити» → «Навчити...»);
- тощо.


**Версія 1.06:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Перемотка: поміняти місцями крайні значення діапазону **зацикленої миті**» → «Перемотка: поміняти місцями крайні значення діапазону **миті, що передслуховується**», «Обрізати медіа, **розмір** хвоста:» → «Обрізати медіа **із розміром** хвоста:»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.05:**
- відредагувала та покращила низку рядків, виправляючи неточності та стилістичні помилки;
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.04:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Зберегти **одиночне меню:**» → «Зберегти **меню [лише одне]:**», «Області редаг. лезом **як один файл**» → «Області редаг. лезом **(як один файл)**»);
- оновила переклад відповідно до нової версії REAPER;
- тощо.


**Версія 1.03:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Прив’язувати за будь-**який** відстані» → «Прив’язувати за будь-**якої** відстані», «Експортувати сполучення клавіш/власні дії **всіх секцій...**» → «Експортувати сполучення клавіш/власні дії **(всі секції)...**»);
- тощо.

**Версія 1.02:**
- відредагувала деякі рядки у розділах actions (наприклад: «Інше: задати темп проєкту за виділенням часу (**із** наявним темпом)» → «Інше: задати темп проєкту за виділенням часу (**з** наявним темпом)»);
- тощо.


**Версія 1.01:**
- вичитала та відредагувала низку рядків для покращення стилістики (наприклад: «Спрямовувати до вводу **віртуальної** міді-клавіатури» → «Спрямовувати до вводу **вірт.** міді-клавіатури», «**Завжди** редагувати елементи поза активним» → «**Дозволити** редагувати елементи поза активним»);
- тощо.


**Версія 1.0:**
- перша публічна не-бета версія.
